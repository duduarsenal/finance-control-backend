import { Router } from "express";

const financeRoute = Router()

// const _financeController = new FinanceController()
// financeRoute.get("/categorias", (req, res) => _financeController.getCategorias(req, res))
// financeRoute.get("/dashboard", (req, res) => _financeController.getDashboardData(req, res))

export { financeRoute }